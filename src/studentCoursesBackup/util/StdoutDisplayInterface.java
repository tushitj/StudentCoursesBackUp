package studentCoursesBackup.util;

public interface StdoutDisplayInterface {
	/**
	 * Function declaration to be included in the class that implements this Interface
	 * this function should write the output to the console
	 */
	void writeToStdout(String s);
}